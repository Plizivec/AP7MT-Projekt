<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTracker.Splash"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MovieTracker.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity definována pro spuštění aplikace -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MovieTracker" />

        <!-- Přidána deklarace pro DetailScreen aktivitu -->
        <activity android:name=".DetailScreen"
            android:exported="true">
            <!-- Možná sem chceš přidat i nějaké další nastavení, jako třeba animace přechodů apod. -->
        </activity>

        <activity android:name=".FavouriteScreen"
            android:exported="true">
            <!-- Možná sem chceš přidat i nějaké další nastavení, jako třeba animace přechodů apod. -->
        </activity>

    </application>

</manifest>
